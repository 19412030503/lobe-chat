const isCreating = (state: any) => state.isCreating;
const isCreatingWithNewTopic = (state: any) => state.isCreatingWithNewTopic;

export const createThreeDSelectors = {
  isCreating,
  isCreatingWithNewTopic,
};
